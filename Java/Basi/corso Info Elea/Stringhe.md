```java
String[] testo = "ciao sono il tuo alias".split(" ",5);
// split() - impostando lo spazio come separatore, restituisce un array
System.out.println(Arrays.toString(testo)); //        [ciao, sono, il, tuo, alias]
```