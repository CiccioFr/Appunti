# Input

```java
Scanner input = new Scanner(System.in);
// accetta in input uno stream di dati
// oggetto predisposto a prendere l'input dello scanner
// il parametro : da dove accettiamo il flusso di dati

System.out.println("Dichiara il tuo nome");
String nome = input.nextLine();
```